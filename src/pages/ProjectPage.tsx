import { Link, useParams } from 'react-router-dom'
import { projects } from '../data/portfolio'
export default function ProjectPage() { const { slug } = useParams(); const project = projects.find(p => p.slug === slug); if (!project) return <main className="project-page"><Link to="/#work">← Back to selected work</Link><h1>Project not found<span>.</span></h1></main>; return <main className="project-page"><Link to="/#work">← Back to selected work</Link><p>{project.number} / {project.type} / {project.year}</p><h1>{project.name}<span>.</span></h1><img src={project.image} alt={`${project.name} 项目封面占位图`} /><p className="detail-copy">{project.description}</p><p className="detail-note">Project detail placeholder. Replace this copy and the cover asset from <code>src/data/portfolio.ts</code> and <code>src/assets</code>.</p></main> }
